import { FilterRentalPipe } from './filter-rental.pipe';

describe('FilterRentalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRentalPipe();
    expect(pipe).toBeTruthy();
  });
});
